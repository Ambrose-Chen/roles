---
# defaults file for git

_git_version: 'git-2.25.1'

_git_repo_url_i: 'https://mirrors.edge.kernel.org/pub/software/scm/git/{{ _git_version }}.tar.gz'
_git_repo_url: 'http://192.168.110.100/software/git/{{ _git_version }}.tar.gz'
_git_repo_name: '{{ _git_version }}.tar.gz'

_git_local_src: '{{ _git_version }}.tar.gz'

_git_dirname: '{{ _git_version }}'
_git_dest: '/opt/'
